{
    "name": "@bsr-comm/server",
    "version": "0.0.1",
    "description": "server",
    "scripts": {
        "build": "tsc",
        "start": "yarn run build && node build/server.js",
        "dev": "CORS_ORIGIN=* HOST=localhost PORT=3000 ./node_modules/nodemon/bin/nodemon.js -e ts  --exec \"yarn run start\""
    },
    "devDependencies": {
        "@types/express": "^4.17.21",
        "@types/node": "^20.4.8",
        "nodemon": "^3.0.1",
        "typescript": "^5.1.6"
    },
    "dependencies": {
        "express": "^4.19.2"
    }
}
